import 'package:flutter/material.dart';

class FloppyScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Floppy Drive'),
      ),
      body: Center(
        child: Text('Floppy Drive Details'),
      ),
    );
  }
} 