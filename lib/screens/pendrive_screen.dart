import 'package:flutter/material.dart';

class PendriveScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Pen Drive'),
      ),
      body: Center(
        child: Text('Pen Drive Details'),
      ),
    );
  }
} 