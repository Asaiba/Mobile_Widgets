import 'package:flutter/material.dart';

class PixelScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Pixel'),
      ),
      body: Center(
        child: Text('Pixel Details'),
      ),
    );
  }
} 