import 'package:flutter/material.dart';

class LaptopScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Laptop'),
      ),
      body: Center(
        child: Text('Laptop Details'),
      ),
    );
  }
}